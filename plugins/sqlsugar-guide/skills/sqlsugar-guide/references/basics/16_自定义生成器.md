# 自定义生成器

下面的方法可用于读取表和列信息：

```csharp
// 例 1：获取所有表
var tables = db.DbMaintenance.GetTableInfoList(false); // true 走缓存，false 不走缓存
foreach (var table in tables)
{
    Console.WriteLine(table.Description); // 输出表信息
}

// 获取列信息
db.DbMaintenance.GetColumnInfosByTableName(表名, false)
```
