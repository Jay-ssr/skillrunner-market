# 数据事务

## 目录

- [单库事务](#单库事务) — Ado.BeginTran/Commit/Rollback
- [多库事务（可跨库）](#多库事务可跨库) — ITenant 多库事务
- [调试事务](#调试事务) / [不一致怎么办](#不一致怎么办)
- [语法糖](#语法糖) — 自动异常、工作单元、使用委托
- [跨方法事务](#跨方法事务)
- [CAP 事务 TCC/Saga](#cap事务-tcc和saga)
- [异步事务](#异步事务)
- [隔离级别](#设置事务隔离级别) / [嵌套事务](#嵌套事务支持异步)
- [工作单元（事务特性）](#工作单元事务特性)
- [MySQL 注意事项](#mysql注意事项)

## 单库事务

单库事务针对单个 db 执行，ISqlSugarClient 和 SqlSugarClient 用法一致：

```csharp
try
{
    db.Ado.BeginTran();
    db.Insertable(new Order() { ..... }).ExecuteCommand();
    db.Insertable(new Order() { ..... }).ExecuteCommand();
    db.Ado.CommitTran();
}
catch (Exception ex)
{
    db.Ado.RollbackTran();
    throw ex;
}
```

单库场景下，多租户事务与单库事务等价，写法更简洁：

```csharp
db.BeginTran();// 去掉了.ado
db.CommitTran();// 去掉了.ado
db.RollbackTran();// 去掉了.ado
// ISqlSugarClient 接口使用多租户事务时，需要先完成多租户配置
```

## 多库事务（可跨库）

多数据库事务是 SqlSugar 独有功能，在单程序中稳定性优于 CAP（CAP 多一层队列）。

SqlSugarClient / SqlSugarScope 继承关系：

```csharp
SqlSugarClient : ISqlSugarClient, ITenant // 多租户声明
```

**多租户声明**：

```csharp
SqlSugarClient db = new SqlSugarClient(new List<ConnectionConfig>()
{
    new ConnectionConfig()
    {
        ConfigId = "0", DbType = DbType.SqlServer, ConnectionString = .., IsAutoCloseConnection = true
    },

    new ConnectionConfig()
    {
        ConfigId = "1", DbType = DbType.MySql, ConnectionString = .., IsAutoCloseConnection = true
    }
});
```

多租户事务与单库事务用法一致，区别仅在于少了 `.Ado`：

| 模式 | 写法 |
|------|------|
| 单库 | `db.Ado.BeginTran` |
| 多库事务 | `db.BeginTran` |
| ISqlSugarClient 多库 | `db.AsTenant().BeginTran()` |

### SqlSugarClient 事务

继承 ITenant 可直接使用（老版本 `var mysql = db.GetConnection` 需写在事务外）：

```csharp
// 禁止使用 db.Ado.BeginTran，多租户是 db.BeginTran
try
{
    db.BeginTran();

    db.GetConnection("1").Insertable(new Order() { }).ExecuteCommand();
    db.GetConnection("0").Insertable(new Order() { }).ExecuteCommand();

    db.CommitTran();
}
catch (Exception)
{
    db.RollbackTran();// 数据回滚
    throw;
}

// 主db：注入的 SqlSugarClient / SqlSugarScope，拥有事务管理和租户方法
// 子db：通过 GetConnection 获取，操作指定数据库，无租户事务方法

// 底层机制：业务成功后统一提交，失败则全部回滚（回滚有 3 次重试）
// 回滚失败会 throw new AggregateException
// 高安全场景：使用差异日志 + Catch(AggregateException ex) 进行补偿
```

### ISqlSugarClient 事务

ISqlSugarClient 未继承 ITenant，需转换：

```csharp
db.AsTenant().BeginTran();// 低版本 (db as ITenant).BeginTran()
db.AsTenant().CommitTran();
db.AsTenant().RollbackTran();
```

## 调试事务

`db.ContextId` 从事务开始到 CURD、事务结束必须一致，事务才生效。MySQL 需确认表引擎支持事务。

## 不一致怎么办？

- SqlSugarClient：通过变量 `var db = 外部Db;` 保证一致
- SqlSugarScope：线程安全对象，单例模式即可保证一致

用 Insert 测试事务（Update 有条件过滤等因素会增加验证难度）：

```csharp
try
{
    db.BeginTran();

    Console.WriteLine(db.Queryable<Order>().Count());// 插入前数量

    db.Insertable(new Order() { CreateTime = DateTime.Now, Name = "aa", Price = 1 }).ExecuteCommand();

    Console.WriteLine(db.Queryable<Order>().Count());// 插入后数量

    throw new Exception("出错");
    db.CommitTran();
}
catch
{
    db.RollbackTran();
    Console.WriteLine(db.Queryable<Order>().Count());// 回滚后数量
}
```

输出结果：插入前和回滚后数量一致即验证成功。

## 语法糖

### 语法糖1：5.0.4才支持

适合有全局异常的场景，出错直接抛出并回滚：

```csharp
using var tran = db.UseTran();// using不能少

db.Queryable<Order>().ToList();
db.Insertable(new Order() { Name = "a" }).ExecuteCommand();

tran.CommitTran();// commitTran提交，如果没有提交成功会自动回滚
```

### 语法糖2：自动异常

适合没有异常处理的场景，减少 try 模板代码：

```csharp
// 不扔出错误
var result = db.UseTran(() =>
{
    var beginCount = db.Queryable<Order>().ToList();
    db.Ado.ExecuteCommand("delete Order");
    var endCount = db.Queryable<Order>().Count();
    return true;// 返回值等行 result.Data
});

if (result.Data == false) // 返回值为false
{
    // result.Data 业务的返回值
    // 如果是上面的逻辑 result.Data == true 肯定业务成功并且事务成功
    // if (result.IsSuccess == false)// 事务执行了回滚
    // if (result.IsSuccess == true)// 事务提交完成
}

// 直接扔出错误
db.UseTran(() =>
{
    var beginCount = db.Queryable<Order>().ToList();
    db.Ado.ExecuteCommand("delete Order");
    var endCount = db.Queryable<Order>().Count();
    return true;
}, ex => { throw ex; });// throw ex
```

### 语法糖3：工作单元

UnitOfWork: https://www.donet5.com/Home/Doc?typeId=2360

## 跨方法事务

- **SqlSugarScope**：单例模式支持跨事务方法
- **SqlSugarClient**：需要 IOC 设置 Scoped 周期实现

## CAP事务 TCC和Saga

### 原理讲解

CAP 支持跨程序间的事务处理（单程序场景优先使用自带多租户事务，CAP 涉及队列稳定性不如原生方案）。

> 注意：MySQL 用户使用需升级到最新。

1. 数据库的自动释放要关闭
2. 手动打开数据库连接 `db.Ado.Connection.Open();`
3. 用 `db.Ado.Connection` 创建事务
4. 把你的事务赋值到 ORM 对象 `db.Ado.Transaction = 你的事务;`
5. 执行你的代码
6. 关闭 Connection 对象

```csharp
// 用户使用案例
var db = GetSqlsugarclient();
// 关闭自动释放（需要 using 手动释放）
db.CurrentConnectionConfig.IsAutoCloseConnection = false;
// 取出ADO事务
using (var connection = (MySqlConnection)db.Ado.Connection)// SqlServer是SqlConnection
{
    using (var transaction = connection.BeginTransaction(_capBus, autoCommit: false))
    {
        if (connection.State != ConnectionState.Open)
        {
            connection.Open();
        }

        db.Ado.Transaction = (IDbTransaction)transaction.DbTransaction;// 这行很重要

        db.Insertable<Test>(new Test()
        {
            name = DateTime.Now.ToString()
        }).ExecuteCommand();

        _capBus.Publish("Sample.RabbitMQ.MySql", DateTime.Now);

        transaction.Commit();
    }
}
```

### CAP作者提供的集成示例

参考官方 issue 中的集成示例；外链不可用时再看下面的备份说明。

## 异步事务

仅在异步方法（返回 Task / Task<T>）中使用以下代码，禁止在同步方法中调用。

**用法1**：

```csharp
try
{
    await db.BeginTranAsync();

    await db.Insertable(new Order()
    {
        CreateTime = DateTime.Now,
        CustomId = 1,
        Name = "aaa",
        Price = 0
    }).ExecuteCommandAsync();

    await db.CommitTranAsync();
}
catch (Exception)
{
    await db.RollbackTranAsync();
}
```

**用法2**：

```csharp
// 只有 5.0.3.8 支持，老版本请升级使用
var res = await db.UseTranAsync(async () =>
{
    await db.Insertable(new Order()
    {
        CreateTime = DateTime.Now,
        CustomId = 1,
        Name = "aaa",
        Price = 0
    }).ExecuteCommandAsync();

    return true;// 返回值会变成 res.Data
});

if (result.Data == false) // 返回值为false
{
    // result.Data 业务的返回值
    // 如果是上面的逻辑 result.Data == true 肯定业务成功并且事务成功
    // if (result.IsSuccess == false)// 事务执行了回滚
    // if (result.IsSuccess == true)// 事务提交完成
}

// 注意:
// await db.UseTranAsync 前面的 await 不要漏掉了
```

## 设置事务隔离级别

**单库模式用法**：

```csharp
try
{
    db.Ado.BeginTran(IsolationLevel.ReadCommitted);

    // 业务代码

    db.Ado.CommitTran();
}
catch (Exception ex)
{
    db.RollbackTran();
    throw ex;
}
```

**多租户模式**：

```csharp
var mysqlDb = db.GetConnection("mysql");
var mssqlDb = db.GetConnection("mssql");
try
{
    mysqlDb.Ado.BeginTran(IsolationLevel.ReadCommitted);// 开启库1的事务
    mssqlDb.Ado.BeginTran(IsolationLevel.ReadCommitted);// 开启库2的事务

    // 业务代码 只能用 mysqlDb 和 mssqlDb

    db.CommitTran();// 注意不能用 db.ado.CommitTran
}
catch (Exception ex)
{
    db.RollbackTran();
    throw ex;
}
```

## 嵌套事务（支持异步）

### 共享模式（推荐）

有外部事务时，内部事务复用外部事务（推荐）。

通过工作单元实现嵌套事务 5.1.2.5-preview03：

```csharp
// db.Ado.IsNoTran() 表示事务为null才开启事务
using (var uow = db.CreateContext(db.Ado.IsNoTran()))
{
    using (var uow2 = db.CreateContext(db.Ado.IsNoTran()))
    {
        uow2.Commit();
    }

    uow.Commit(); // 禁止用 db.RollBack 工作单元内只要throw会自动回滚
}
```

### 子事务独立（不推荐）

业务 A 和 B 操作相同表时易死锁，不推荐：

```csharp
try
{
    db.BeginTran();
    // 业务..A

    var childDb = db.CopyNew();
    try
    {
        childDb.BeginTran();
        // ...业务B
        childDb.CommitTran();
    }
    catch (Exception)
    {
        childDb.RollbackTran();// 数据回滚
        throw;
    }

    db.CommitTran();
}
catch (Exception)
{
    db.RollbackTran();// 数据回滚
    throw;
}
```

**事务嵌套**：使用本节前面的嵌套事务方案，避免使用旧写法。

## 工作单元【事务特性】

```csharp
[ServiceFilter(typeof(TransactionFilter))]// 加上这行就可以用了
public IEnumerable<WeatherForecast> Get()
{
    .....数据库操作...
}
```

- 详见：https://www.donet5.com/Doc/27/2537

## MySQL注意事项

1. **MYSQL 不支持创建表和删除表处理事务**，原生事务也一样
2. **MyISAM 存储引擎不支持事务**，需要改成 InnoDB
